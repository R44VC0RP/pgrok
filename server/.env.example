# pgrok server configuration
# No API tokens needed — Caddy uses HTTP-01 challenge for certificates
# which only requires ports 80/443 to be open.

# Domain for tunnel subdomains (used by pgrok-tunnel script)
PGROK_DOMAIN=yourdomain.com
